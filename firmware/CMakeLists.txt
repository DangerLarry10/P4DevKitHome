# P4DevKitHome - Custom Home Screen for Waveshare ESP32-P4 DevKit
# This is the top-level CMake file that ESP-IDF's build system expects.
# It just tells ESP-IDF "this folder is a project" and gives it a name.
cmake_minimum_required(VERSION 3.5)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(p4devkithome)
